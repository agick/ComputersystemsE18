#include "sensor.h"
#include <stdio.h>
#include <stdlib.h>

int getNextData(FILE *file)
{
   return 0; // return sensor value
}

FILE* openfile(const char* filename)
{
   FILE *file = filename;
   return file;
}
